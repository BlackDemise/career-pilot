Review the following CV. Return only valid JSON matching this schema:
{"overallAssessment":"string","strengths":["string"],"weaknesses":["string"],"recommendations":["string"]}
Do not wrap the JSON in Markdown code fences.

CV:
{{cv}}
