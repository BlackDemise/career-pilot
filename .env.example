POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

# Google Gemini API key (server-side only, never exposed to the frontend).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Single dev/basic-auth user seeded on backend startup (MVP has no registration flow yet).
DEV_USER_USERNAME=admin
DEV_USER_PASSWORD=changeme