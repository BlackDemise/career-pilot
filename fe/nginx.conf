server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback so client-side routes don't 404 on refresh/direct navigation
        try_files $uri $uri/ /index.html;
    }
}
